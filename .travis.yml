language: node_js
sudo: false
cache:
  directories:
    - node
dist: trusty
node_js:
  - '7.3.0'
branches:
  only:
  - master
before_install:
 - npm install -g @angular/cli@latest
script:
 - ng build
notifications:
  webhooks:
    on_success: always
    on_failure: always
    on_start: false